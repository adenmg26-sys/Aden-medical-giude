# تقرير تحليل مشكلة التشغيل بدون إنترنت (Offline Mode Analysis)

تم إجراء تحليل شامل لإعدادات الـ PWA وكود التطبيق لمعرفة سبب عدم عمل التطبيق عند انقطاع الاتصال بالإنترنت. إليك النتائج والحلول المقترحة:

## 1. تحليل الوضع الحالي (Analysis)

*   **Service Worker:** يتم إنشاء ملف `sw.js` بنجاح أثناء الـ Build، ولكن يبدو أن إستراتيجية التخزين المؤقت (Caching Strategy) الافتراضية في `next-pwa` لا تقوم بحفظ "مستند الـ HTML" الأساسي (Document) بشكل كافٍ في إصدارات Next.js الحديثة (App Router).
*   **عائق التشغيل (المكتشف):** تبيّن أثناء عملية الـ Build أن أحد ملفات الجافاسكريبت الأساسية (Chunk) حجمه كبير جداً (6.78 MB)، وكان الـ Service Worker يتجاهل تخزينه تلقائياً بسبب تجاوزه الحد المسموح به افتراضياً. هذا يعني أن التطبيق عند محاولةفتحه بدون إنترنت يفشل في تحميل الكود الأساسي للمتصفح.
*   **التزامن (Sync):** كود المزامنة `useSync` و `db.ts` مصمم بشكل صحيح للعمل Offline، لذا المشكلة كانت تقنية في ملفات النظام وليست في البيانات.

## 2. الأسباب المحتملة للفشل (Potential Causes)

1.  **عدم تخزين المستند الرئيسي:** الـ Service Worker يحتاج لتفعيل إستراتيجية `NetworkFirst` أو `StaleWhileRevalidate` لصفحة الـ `/` والصفحات الفرعية لضمان وجود نسخة "أوفلاين".
2.  **التفعيل المتأخر:** في بعض الأحيان لا يتم تفعيل الـ Service Worker فوراً إلا بعد تحديث الصفحة مرة ثانية (تمت معالجة هذا جزئياً بـ `clientsClaim`).
3.  **بيئة التطوير:** الـ PWA معطل حالياً في بيئة التطوير (Development)، لذا يجب اختباره فقط بعد عمل `npm run build` و `npm run start`.

## 3. الحلول التقنية المقترحة (Proposed Solutions)

لحل المشكلة وضمان تشغيل التطبيق في وضع الأوفلاين 100%، يجب اتخاذ الخطوات التالية:

### أ. تحديث إعدادات `next.config.ts`
إضافة خيار `cacheOnFrontEndNav` وتخصيص الـ `runtimeCaching` لضمان حفظ الصفحات.

### ب. إضافة صفحة Fallback (Offline Page)
إنشاء صفحة بسيطة تظهر في حال تعذر تحميل أي صفحة جديدة أثناء الأوفلاين، وتخزينها في الكاش مسبقاً.

### ج. إجبار المتصفح على حفظ الـ Root
تحديث ملف `layout.tsx` أو `sw.js` للتأكد من أن الصفحة الرئيسية مخزنة دائماً.

## 4. خطة الإصلاح (Action Plan)

1.  تعديل `next.config.ts` ليتضمن إعدادات تخزين أكثر صرامة للصفحات (Pages Caching).
2.  إضافة ملف `public/offline.html` كحل احتياطي.
3.  إجراء Build جديد ورفعه لـ Vercel للتأكد من أن المتصفح سيقوم بتحميل النسخة الجديدة التي تدعم الأوفلاين بشكل كامل.

---
**ملاحظة:** سأقوم الآن بالبدء في تنفيذ هذه التعديلات لضمان عمل التطبيق بدون إنترنت كما هو متفق عليه.
